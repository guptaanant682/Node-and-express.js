console.log("This is nodejs tutorial for me");

// process.exit(0); // This process has a lot of method but we are using exit now. By default the exit code is 0 for successful exit 
// process.exit(1); // it crashes our application
